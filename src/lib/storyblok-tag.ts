// Standalone cache-tag constant (no heavy imports), so route handlers can tag /
// revalidate Storyblok reads without pulling in the full Storyblok component map.
export const STORYBLOK_TAG = "storyblok";
