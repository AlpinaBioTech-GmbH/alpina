// Single knob for tone across every social platform. Composers and validators
// both import these constants so they agree. The actual copy lives in
// content.config.ts (content.social.*) so a rebrand never touches this file.
import { content } from "@/lib/config";

export const PERSONA = content.social.persona;
export const ANGLE_PRINCIPLE = content.social.anglePrinciple;
export const ACCURACY_RULE = content.social.accuracyRule;
